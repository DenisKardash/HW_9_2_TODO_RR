const searchTextInitialState = [];

export const searchTextReducer = (state = searchTextInitialState, { type, payload }, searchText) => {
	switch (type) {
		case 'SEARCH_TODOS':
		return 

		default:
			return state;
	}
};
